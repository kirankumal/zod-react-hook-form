import { FC } from "react";

export const SubmitInput: FC = () => {
  return (
    <input
      type="submit"
      value="Submit"
      className="bg-blue-400 py-2 px-3 rounded cursor-pointer text-white"
    />
  );
};
